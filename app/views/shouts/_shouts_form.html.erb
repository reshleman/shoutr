<%= form_for shout do |form| %>
  <%= render "errors", object: shout %>

  <div>
    <%= form.label :content %>
    <%= form.text_field :content, placeholder: "Say it here!" %>
  </div>
  <div>
    <%= form.submit "Shout!" %>
  </div>
<% end %>
