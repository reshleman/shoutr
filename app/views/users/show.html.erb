<section class="sidebar">
  <header>
    <h1>Following</h1>
  </header>

  <div class="users">
    <%= render @user.followed_users %>
  </div>
</section>

<section class="timeline">
  <header>
    <h1><%= @user.username %>'s Shouts</h1>
  </header>

  <div class="shouts">
    <%= render @shouts %>
  </div>
</section>
